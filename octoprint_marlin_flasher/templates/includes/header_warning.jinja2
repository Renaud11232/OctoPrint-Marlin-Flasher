<div class="alert">
    <span class="label label-important">{{ _('Important') }}</span> {{ _("In order to properly use Marlin Flasher, you'll need to increase the maximum request size.") }}
    {{ _('If it has not been done yet, simply edit <code>config.yml</code>.') }}
    <pre>
server:
  maxSize: 104857600</pre>
</div>
